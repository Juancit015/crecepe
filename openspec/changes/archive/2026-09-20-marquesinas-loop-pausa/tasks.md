## 1. Loop continuo unidireccional

- [x] 1.1 Cambiar ambas pistas a keyframes de loop `linear` infinito (`0 → -50%`) con duraciones 40s especialidades y 30s garantías, verificando movimiento constante en una sola dirección sin saltos ni pausas en los extremos
- [x] 1.2 Agregar pausa en hover por barra (`animation-play-state`) con reanudación desde el punto exacto, verificando que al retirar el mouse el movimiento continúa

## 2. Contenido y verificación

- [x] 2.1 Agregar "Soluciones Digitales" en ambos grupos de la pista de especialidades y verificar simetría exacta de grupos, `prefers-reduced-motion` intacto y `openspec validate marquesinas-loop-pausa` en válido
