## 1. Bloqueo total de fondo y hamburguesa a la derecha

- [x] 1.1 Bloquear `overflow` en `html` + `body` al abrir y restaurar ambos en `closeMenu()` en `assets/js/main.js`; verificar cierre limpio al redimensionar a desktop
- [x] 1.2 Reordenar móvil en `assets/css/styles.css` (logo izquierda, hamburguesa con borde a la derecha); verificar móvil, ventana angosta de escritorio y ambos temas; validar con `openspec validate menu-scroll-lock`
