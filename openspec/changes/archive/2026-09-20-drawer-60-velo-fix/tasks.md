## 1. Velo que no tapa + drawer 60%

- [x] 1.1 Bajar el velo a `z-index: 999`, devolver el panel a `1000` y poner ancho `min(60vw, 340px)`, y verificar que el panel abre nítido e interactivo.
- [x] 1.2 Atenuar logo y hamburguesa con `opacity` al abrir (solo hojas, nunca contenedores) y verificar dim total, cierre desde velo/links/X/hamburguesa y ambos temas.
- [x] 1.3 Quitar el fondo sólido forzado a la navbar al abrir (solo dim, sin blur que atrape al panel) y verificar en hero que la navbar queda transparente.
