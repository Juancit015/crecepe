## 1. Auditoría + paleta oscura

- [x] 1.1 Listar todas las reglas `body.dark-mode` que tocan precios en `assets/css/styles.css` y decidir por cada una si se conserva, absorbe o borra
- [x] 1.2 Escribir el bloque único `/* Precios en oscuro */` con la paleta fija (fondos, textos, badges, CTAs + hovers), regenerar `styles.min.css` y verificar llaves balanceadas

## 2. Verificación y cierre

- [x] 2.1 Verificar en móvil y desktop en oscuro: 3 cards oscuras con la del medio destacada, textos legibles, CTAs distinguibles con sus hovers, y modo claro intacto
- [x] 2.2 Documentar en `CHANGELOG.md`, validar con `openspec validate --strict` y commitear
