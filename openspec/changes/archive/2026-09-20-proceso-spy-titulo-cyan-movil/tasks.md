## 1. Título cyan en el spy móvil

- [x] 1.1 Añadir en `assets/css/styles.css` la regla `.process-step.active .process-step-body h4` con el mismo cyan del hover, y verificar en emulación móvil que el título se tiñe al cruzar el centro y se apaga al salir.
- [x] 1.2 Verificar que en desktop nada cambia (spy apagado, solo hover) y que `prefers-reduced-motion` sigue sin glow ni cyan en ambos modos.
