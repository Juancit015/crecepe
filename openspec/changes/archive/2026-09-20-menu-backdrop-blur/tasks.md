## 1. Velo con blur, cierre al tocar fuera y bloqueo de scroll

- [x] 1.1 Agregar `.nav-backdrop` tras la navbar en `index.html` y sus estilos (blur + dim, `z-index: 999`) en `assets/css/styles.css`
- [x] 1.2 Conectar el velo a `closeMenu()` en `assets/js/main.js`, verificar bloqueo de scroll y cierre (toque fuera, link, Escape, resize) en móvil y ambos temas; validar con `openspec validate menu-backdrop-blur`
