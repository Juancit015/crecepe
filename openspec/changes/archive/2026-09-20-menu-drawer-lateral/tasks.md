## 1. Drawer lateral derecho con links iconizados

- [x] 1.1 Convertir `#navLinks` en drawer lateral (fondo marino, X interna, 7 iconos SVG, divisores, CTA y tema adaptados) en las 8 páginas y `assets/css/styles.css`
- [x] 1.2 Conectar la X a `closeMenu()` en `assets/js/main.js` si hace falta; verificar móvil y ambos temas (apertura lateral, velo, cierres, scroll bloqueado) y desktop intacto; validar con `openspec validate menu-drawer-lateral`

## 2. Drawer visible completo al abrir

- [x] 2.1 Diagnosticar por qué el contenido del drawer ño se ve (transición, caché o altura) y corregirlo en `assets/css/styles.css` (`main.js` solo si hace falta)
- [x] 2.2 Verificar en móvil y ambos temas que X, links, CTA y tema aparecen completos; validar con `openspec validate menu-drawer-lateral`
