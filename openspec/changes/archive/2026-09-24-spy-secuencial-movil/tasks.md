## 1. Spy determinista en móvil

- [x] 1.1 Reescribir `onEntries` en `opinionSpy` y `processSpy` (ganador por cercanía al centro) y verificar con `node --check` que el JS sigue válido
- [x] 1.2 Verificar scroll lento (1→2→3→4 en orden), scroll rápido (queda el más cercano al centro, sin saltos ni dobles) y salida de franja (se apaga), en pasos y QA de fichas

## 2. Cierre

- [x] 2.1 Bump `?v` de assets en las 8 páginas + `tools/build_pages.py`, regenerar `styles.min.css`, registrar en `CHANGELOG.md` y commitear
