## 1. Retiro de la mini-barra

- [x] 1.1 Quitar markup `.buybar` en las 3 fichas, su CSS (incluido dial y `reduce`) y el observer en `main.js`, y verificar con `grep` cero referencias a `buybar` en código
- [x] 1.2 Verificar card fija intacta, dial y flecha normales, sin scroll horizontal y desktop intacto

## 2. Cierre

- [ ] 2.1 Bump `?v` de assets en las 8 páginas + `tools/build_pages.py`, regenerar `styles.min.css`, registrar en `CHANGELOG.md` y commitear
