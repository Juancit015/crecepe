## 1. Acordeón + breakpoint tablet

- [ ] 1.1 Calibrar `max-height` colapsado a 3 items exactos + aire bajo el fade en `.pricing-card .pricing-features` móvil, regenerar `styles.min.css` y verificar a ~650px (captura 16-34-25: preview limpio, CTA separado)
- [ ] 1.2 Agregar media ~900px con `opiniones-grid` a 2 col (1 si no respira) y verificar a 800/834/1024px; geo, Proceso y desktop sin cambios, claro/oscuro

## 2. Lanzamiento y documentación

- [ ] 2.1 Bump `?v` de assets en las 8 páginas + `tools/build_pages.py` (Ño regenerar páginas), registrar en `CHANGELOG.md` y commitear
- [ ] 2.2 Sync de deltas a `planes-entrada` y `opiniones`, archivar el change y reportar
