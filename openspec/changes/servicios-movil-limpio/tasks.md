## 1. Limpieza y centrado

- [x] 1.1 Devolver `.service-photo .service-overlay` a `display:none` en móvil, centrar `.service-photo` con `align-self:center`, regenerar `styles.min.css` y verificar llaves
- [x] 1.2 Poner `.service-link` y `.pricing-toggle` de planes en blanco con hover cyan en móvil (verificando legibilidad sobre el fondo real en claro y oscuro), regenerar y verificar

## 2. Verificación y cierre

- [x] 2.1 Verificar en móvil (claro y oscuro): fotos limpias y centradas, atajos blancos con hover cyan, caption de casos intacta
- [ ] 2.2 Documentar en `CHANGELOG.md`, validar con `openspec validate --strict` y commitear
