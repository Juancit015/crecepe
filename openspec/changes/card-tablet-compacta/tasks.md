## 1. Card contenida en tablet

- [x] 1.1 Aplicar `max-width` + centrado al aside en el bloque `@media (max-width: 992px)` y decidir si compactar type/padding comparando con la referencia, y verificar en ~768px que la card se ve contenida como la de AZ
- [x] 1.2 Verificar en ~360px (proporcionada a una columna), orden QA → card → FAQ intacto, sin scroll horizontal y desktop sin cambios

## 2. Cierre

- [ ] 2.1 Bump `?v` de assets en las 8 páginas + `tools/build_pages.py`, regenerar `styles.min.css`, registrar en `CHANGELOG.md` y commitear
